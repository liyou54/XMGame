unmanaged 


1 按方法结构生成部分类 做好拆分
1.1 按照类型生成对应的文件
1.2 对于 val[包含嵌套] 类型的字段 生成 XBlobptr<T> 以及 CfgI<T>
1.3 支持容器嵌套
1.4 对于link类型 生成 xx_ParentPtr xx_ParentIndex
1.5 生成索引结构体 包含构造方法 以及 GetVal<T>(this index) 或者 GetVal<nativearray<T>>(this index,alloc)
特别注意 生成方法使用c# 注意封装 生成方法不允许有魔法数字 最好所有方法使用 函数 +静态变量 使用栈进行缩进 保持生成主方法干净 提取工具方法